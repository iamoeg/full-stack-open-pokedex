name: Hello Full Stack Open!

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-22.04
    steps:
      - name: Say hello
        run: echo "Hello Full Stack Open!"
      - name: Print current date
        run: date
      - name: List directory content
        run: ls -alh
